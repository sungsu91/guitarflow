export default function PracticeBeatDots({meter=[4,4],beat=-1,showMeter=true}){return <div className="etudeBeatRow" aria-label={meter.join('/')+' · '+(beat<0?'정지':(beat+1)+'박')} data-meter={meter.join('/')} data-beat={beat}>{Array.from({length:meter[0]},(_,i)=><span key={i} className={`etudeBeat ${beat===i?'is-on':''} ${i===0?'is-downbeat':''}`}><i/>{i+1}</span>)}{showMeter&&<small>{meter.join('/')}</small>}</div>;}
