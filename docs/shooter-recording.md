# 슈팅게임 촬영모드

촬영모드 버튼 → 전면 카메라·마이크 권한 → 구도 확인 → REC → ■ → 합성 영상 확인 → 영상 저장.

모바일 촬영 중에는 기존 게임 패널과 아레나 크기·맵 구성을 유지한다. 하단 네비게이션을 조건부로 제거하고, 기존 게임 높이의 12.5%까지 카메라를 위로 확장한다. 촬영 중에만 기타·보관함·펫·라이프를 같은 거리만큼 올리고 종료 시 복원한다. 맵 크기는 유지하고 저장 영상에서 카메라가 덮는 하단만 잘라낸다. 조준 기준 캐시는 기타 위치 변경 시 갱신하며 펫 드래그는 이동된 기준점을 보정한다. 게임 영역을 덮는 이동·크기 조절 창은 모바일에서 제거했다. 촬영 종료 시 네비게이션이 돌아온다. 데스크톱은 별도의 기존 이동·크기 조절 카메라 UI를 유지한다.

모바일 카메라는 `contain` 방식으로 원본 전체를 보여주므로 좌우 여백이 생길 수 있다. 저장 영상도 같은 게임/카메라 경계와 비율, 좌우 반전을 사용한다. 데스크톱 카메라는 기존 `cover` 방식을 유지한다. 결과 확인 영상은 하단 버튼 영역을 제외한 공간에 contain 방식으로 맞춰 전체가 보인다. 다시 촬영·저장·종료 버튼은 아래에 별도로 배치한다. 데스크톱 전용 카메라 레이아웃과 모바일 전용 레이아웃은 별도 컴포넌트이고 미디어 상태·저장 로직은 공유한다.

## 기존 게임 보호

- App에는 촬영 컴포넌트와 네비게이션·독립 음정 모니터의 조건부 렌더링만 연결한다.
- 촬영 중 게임 패널의 크기나 스타일을 변경하지 않는다. 게임 DOM을 다른 부모로 이동하거나 다시 마운트하지 않는다.
- 적·발사체·충돌·피치 판정·타이밍·난이도·점수 로직은 변경하지 않는다.
- 게임이 사용 중인 마이크의 오디오 트랙을 복제한다. 촬영 종료는 복제본만 정리하며 원래 게임 마이크를 종료하지 않는다. 마이크가 아직 없다면 기존 게임의 마이크 시작 경로를 이용한다.
- 게임 사운드는 공유 오디오 버스의 limiter에서 별도의 녹음 destination으로 병렬 연결한다. 마이크와 이 출력을 한 오디오 트랙으로 합친다. 병렬 연결에 실패하면 마이크만 녹음한다. 원래 출력 연결과 AudioContext는 종료하지 않는다.

## 영상 합성과 현재 제한

**현재 게임은 canvas가 아니라 DOM/CSS 렌더러다. 이 구현은 화면 녹화와 동일한 품질·프레임률을 보장하지 않는다.**

게임의 현재 계산된 스타일·텍스트·이미지를 독립 iframe에 복사하고 html2canvas로 래스터화한다. 카메라 프레임과 이를 하나의 canvas에 위·아래로 합성한 뒤 `captureStream(30)`과 MediaRecorder로 저장한다. 프리뷰에서 REC를 누르기 전에는 게임 화면 캡처를 실행하지 않으며 캡처 모듈도 지연 로드한다.

- 게임 스냅샷은 동시에 하나만 생성한다. 한 프레임 생성 후 최소 125ms, 또는 생성 시간의 두 배만큼 쉬므로 게임 캡처는 최대 8fps 미만이다. 복잡한 맵·느린 기기에서는 1fps 이하가 될 수 있다. 카메라 합성 루프의 30fps 설정은 실제 저장 프레임률 보장이 아니다.
- 전체 앱의 큰 스타일시트나 비활성 화면을 반복 복제하지 않는다. 그래도 DOM 스타일 읽기·복사·래스터화 비용은 발생한다. **촬영 중 렌더링·입력 지연이 전혀 증가하지 않는다는 요구까지 검증·충족한 상태는 아니다.**
- CSS 필터, 혼합 모드, 마스크, 복잡한 3D 변환·의사 요소 등은 실제 화면과 다르게 저장될 수 있다. 모든 스킨과 맵의 픽셀 일치를 보장하지 않는다.
- 브라우저 화면 공유를 요구하지 않는다. 모바일에서 정확한 화면을 실시간으로 캡처하는 범용 웹 API가 없으므로, 높은 프레임률과 모든 효과의 동일한 재현이 필수라면 공유 렌더러의 캡처 가능한 출력 설계 또는 네이티브 녹화 경로가 별도 작업으로 필요하다.
- 처음 REC를 누르면 카메라·이미지 준비를 기다린다. 준비가 완료되어 MediaRecorder가 시작된 시점부터 녹화 시간을 표시한다.
- 메모리 보호를 위해 10분 또는 수집된 영상 조각 200MiB에서 자동 중지하고 결과 확인 화면을 제공한다. 서버 업로드나 자동 사진 앱 저장은 없다.
- 녹화 시작 시 출력 화면 비율을 정한다. 녹화 도중 화면 크기를 바꾸더라도 저장 파일의 해상도는 유지된다.

## Safari / iPhone / PWA

HTTPS 또는 localhost에서 사용한다. 실제 API 존재 여부를 검사하고 MP4/H.264/AAC, MP4, WebM/VP8/Opus, WebM 순으로 지원 형식을 선택한다. 명시적 MIME 형식을 지원하지 않는 구현에서는 브라우저 기본 설정을 시도하고 실패하면 게임 화면으로 복귀한다.

사용자가 영상 저장을 누르면 파일 공유 지원 환경에서는 시스템 공유창을 사용한다. 그렇지 않으면 다운로드 링크를 실행한다. 공유 취소 시 영상은 유지한다. 공유 기능이 노출되어 있지만 실제 공유가 실패하는 브라우저에서는 다운로드를 시도한다. 다운로드 요청 이후 OS에 실제로 파일이 저장되었는지는 웹 앱이 확정할 수 없으므로 UI도 저장 완료로 단정하지 않는다.

Safari의 MediaRecorder와 생성된 Canvas/Web Audio 트랙 녹화 지원은 [WebKit 공식 설명](https://webkit.org/blog/11353/mediarecorder-api/)을 참고했다. Safari 18.4의 WebM 녹화 추가는 [WebKit 릴리스 설명](https://webkit.org/blog/16574/webkit-features-in-safari-18-4/)을 참고했다. 화면 캡처의 브라우저 제약은 [MDN getDisplayMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getDisplayMedia), DOM 캡처의 재현 범위는 [html2canvas 설명](https://html2canvas.hertzen.com/documentation/) 및 [지원 기능](https://html2canvas.hertzen.com/features/)을 참고한다.

**iPhone 실기기 Safari와 설치형 PWA는 이 개발 환경에서 검증하지 못했다.** 아래 자동 검증은 Chromium과 합성 카메라·마이크를 사용했다. 실제 iPhone에서는 카메라·기타 입력 동시 사용, 오디오 믹스, 잠금·전화 수신, 시스템 공유창에서 사진/파일 저장, 긴 영상의 메모리 사용량을 추가 확인해야 한다. 이 기록을 실기기 호환성 보증으로 해석하면 안 된다.

## 정리와 오류 복귀

카메라·마이크 거부, 미지원, 프리뷰 오류, 준비 시간 초과, 녹화 오류, 빈 결과, Blob URL 생성 실패, 장치 연결 종료, 페이지 전환 시 촬영 자원을 해제하고 원래 레이아웃으로 복귀한다. 늦게 완료되는 권한 요청에도 세대 번호를 확인해 반환된 스트림을 즉시 종료한다.

정리 대상은 MediaRecorder 이벤트, 촬영용 트랙, 오디오 병렬 연결, 합성 스트림, iframe, 타이머, animation frame, Object URL이다. 결과 확인 중 공유창 취소·일시적 백그라운드 진입은 결과를 삭제하지 않는다. 저장 실패도 결과를 유지해 재시도할 수 있게 한다.

## 검증

- `npm run build`: 성공. 기존 큰 번들에 대한 Vite 경고는 남아 있다.
- 촬영·충돌·게임 규칙·피치 판정·모바일 좌표계 관련 선택 테스트 33개 통과.
- 전체 테스트의 실패 13개는 모두 HEAD 기반 별도 비교에서도 실패한 항목이다. 촬영 변경으로 새로 추가된 실패는 없었다.
- `scripts/verify-shooter-recording-browser.mjs`: 모바일 390×844 및 데스크톱 1366×768에서 실제 MediaRecorder 파일 생성·디코딩·다운로드, 오디오/비디오 트랙, 재촬영·URL 해제, 게임 내부 크기·네비게이션 복원, 촬영 트랙 종료와 게임 마이크 유지 확인.
- 카메라 거부, 마이크 거부, MediaRecorder 미지원, 생성자 오류, 늦은 권한 응답, 화면 전환, 녹화 오류, 카메라 연결 종료, 빈 결과, URL 생성 실패도 검사한다.

QA 스크립트에는 별도의 Playwright 설치가 필요하다. `PLAYWRIGHT_MODULE`에 기존 Playwright 모듈의 file URL, `CHROME_PATH`에 브라우저 경로, `RECORDING_TEST_URL`에 로컬 서버 주소를 지정할 수 있다. 결과는 `artifacts/shooter-recording/verification.json` 및 해당 디렉터리의 이미지·영상에 저장된다. 이 샘플의 녹색 카메라는 자동 검사용 가상 장치다.

