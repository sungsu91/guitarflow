import assert from 'node:assert/strict';
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE);const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));
try{await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('button',{name:'간단 악보 만들기',exact:true}).click();assert.ok(await p.locator('.etudeEditorCanvas').evaluate(e=>document.activeElement===e));const press=k=>p.keyboard.press(k);const read=async()=>{await press('Control+s');return p.evaluate(()=>Object.values(JSON.parse(localStorage.getItem('fretiva.etude.library.v2')).records)[0].document)};
for(const key of ['2','ArrowRight','3','ArrowUp','4','ArrowDown','ArrowLeft','5'])await press(key);let d=await read();assert.equal(d.measures[0].events[0].notes[0].fret,5);assert.deepEqual(d.measures[0].events[1].notes.map(n=>[n.string,n.fret]),[[6,3],[5,4]]);
await p.getByRole('button',{name:'실행 취소',exact:true}).click();await press('ArrowRight');await press('6');d=await read();assert.equal(d.measures[0].events[0].notes[0].fret,2);assert.equal(d.measures[0].events[1].notes.find(n=>n.string===6).fret,6);assert.ok(d.measures[0].events[2].rest);
await p.getByRole('button',{name:'이 브라우저에 저장',exact:true}).click();await press('Delete');await press('Control+z');d=await read();assert.equal(d.measures[0].events[1].notes.find(n=>n.string===6).fret,6);await press('Control+Shift+z');d=await read();assert.equal(d.measures[0].events[1].notes.length,1);await press('Control+z');
const field=p.getByLabel('빠른 프렛 입력',{exact:true});await field.focus();await press('ArrowUp');assert.ok(await field.evaluate(e=>document.activeElement===e));assert.equal(await field.inputValue(),'7');
await p.getByRole('button',{name:'이 브라우저에 저장',exact:true}).click();await press('+');d=await read();assert.equal(d.measures[0].events[1].duration,'8');assert.deepEqual(errors,[]);console.log('PASS: initial focus, arrows, toolbar keyboard continuation, numbers stay, Delete/Undo/Redo, duration, form field isolation');}finally{await b.close()}
