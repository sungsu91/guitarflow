import assert from 'node:assert/strict';
import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});
try {for(const width of [390,1440]){const p=await b.newPage({viewport:{width,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('button',{name:'악보 만들기',exact:true}).click();await p.getByRole('button',{name:'프렛 3',exact:true}).click();await p.getByRole('button',{name:'빔표기 도구 열기',exact:true}).click();const below=p.getByRole('button',{name:'TAB 빔 아래 표시',exact:true}),above=p.getByRole('button',{name:'TAB 빔 위 표시',exact:true}),short=p.getByRole('button',{name:'TAB 빔 짧은 기둥 표시',exact:true});assert.equal(await below.getAttribute('aria-pressed'),'true');await above.click();await short.click();assert.equal(await above.getAttribute('aria-pressed'),'true');assert.equal(await short.getAttribute('aria-pressed'),'true');await below.click();assert.equal(await short.getAttribute('aria-pressed'),'true');assert.deepEqual(errors,[]);console.log('PASS editor opens and independent beam settings',width);await p.close();}}finally{await b.close();}
