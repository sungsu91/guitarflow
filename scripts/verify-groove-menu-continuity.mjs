import assert from 'node:assert/strict';
import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});const p=await b.newPage({viewport:{width:390,height:844}});await p.addInitScript(()=>{window.hits=[];window.stops=[];const start=AudioBufferSourceNode.prototype.start,stop=AudioBufferSourceNode.prototype.stop;AudioBufferSourceNode.prototype.start=function(t,...a){window.hits.push({t,now:this.context.currentTime});return start.call(this,t,...a)};AudioBufferSourceNode.prototype.stop=function(t,...a){window.stops.push({t,now:this.context.currentTime});return stop.call(this,t,...a)};});await p.goto('http://127.0.0.1:5173/#metronome');await p.getByRole('button',{name:'3 그루브',exact:true}).click();await p.locator('button').filter({hasText:/^PLAY$/}).click();await p.waitForTimeout(1500);const c=await p.context().newCDPSession(p);await c.send('Emulation.setCPUThrottlingRate',{rate:6});await p.getByRole('button',{name:'메뉴 열기',exact:true}).click();await p.getByText('사운드 및 리듬 설정',{exact:true}).click();assert.equal(await p.getByRole('slider',{name:'그루브팩 볼륨',exact:true}).inputValue(),'70');for(let i=0;i<15;i++){await p.getByRole('slider',{name:'그루브팩 볼륨',exact:true}).fill(String(40+i*3));await p.waitForTimeout(60);}await p.waitForTimeout(1000);await p.locator('#utility-menu-panel').getByRole('button',{name:'메뉴 닫기',exact:true}).click();await p.waitForTimeout(4500);const result=await p.evaluate(()=>({hits:window.hits,earlyStops:window.stops.filter(x=>x.t-x.now<.01)}));const times=[...new Set(result.hits.map(x=>x.t))].sort((a,b)=>a-b);assert.ok(times.length>20);for(let i=1;i<times.length;i++)assert.ok(times[i]-times[i-1]<.376,`Missing beat: ${times[i-1]} to ${times[i]}`);assert.equal(result.earlyStops.length,0);console.log('PASS: menu open/close and 15 volume changes at 6x CPU slowdown, no missing groove beats; default volume 70%');await b.close();

