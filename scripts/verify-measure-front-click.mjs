import assert from 'node:assert/strict';import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('tab',{name:'에튀드',exact:true}).click();const first=p.locator('[data-start-bar="0"]');await first.waitFor();for(const index of [1,0]){const target=p.locator(`[data-start-bar="${index}"]`);await target.scrollIntoViewIfNeeded();const box=await target.boundingBox();await p.mouse.click(box.x+5,box.y+18);await p.waitForTimeout(200);assert.match(await target.getAttribute('fill'),/rgba/);}console.log('PASS: tapping measure front margin selects its first beat');}finally{await b.close();}
