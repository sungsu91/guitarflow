import assert from 'node:assert/strict';
import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});
try{const page=await browser.newPage({viewport:{width:390,height:844}});await page.goto('http://127.0.0.1:5173/#etudes');await page.getByRole('button',{name:'악보 만들기',exact:true}).click();for(const instrument of ['guitar','bass','ukulele','drums','piano']){await page.getByLabel('악기 선택',{exact:true}).selectOption(instrument);const both=page.getByRole('button',{name:'오선보+TAB',exact:true});if(await both.count())await both.click();const hit=page.locator('.etudeEditorHit[data-mode="staff"]').first();await hit.waitFor({state:'attached'});await hit.dispatchEvent('click');if(instrument!=='piano'){assert.equal(await page.locator('[data-staff-editable="false"] .etudeInputCursor').count(),instrument==='drums'?0:await page.locator('.etudeInputCursor').count());assert.equal(await page.locator('[data-staff-editable="true"]').count(),0);}else assert.ok(await page.locator('[data-staff-editable="true"]').count());console.log('PASS',instrument);} }finally{await browser.close();}
