import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{for(const width of [375,390]){const p=await b.newPage({viewport:{width,height:844}});await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('button',{name:'악보 만들기',exact:true}).click();await p.getByRole('button',{name:'프렛 3',exact:true}).click();await p.getByRole('button',{name:'주법 도구 열기',exact:true}).click();const panel=p.locator('#mobile-tool-note');await panel.waitFor();assert.ok(await panel.evaluate(e=>e.scrollWidth<=e.clientWidth+1));const boxes=await panel.locator('.techniqueGroup button').evaluateAll(es=>es.map(e=>e.getBoundingClientRect().toJSON()));assert.ok(boxes.every(r=>r.height>=44));await panel.getByRole('tab',{name:'½',exact:true}).click();assert.equal(await panel.getByRole('group',{name:'벤드와 릴리스',exact:true}).count(),1);assert.equal(await panel.getByRole('group',{name:'음 연결',exact:true}).count(),0);await panel.getByRole('tab',{name:'일반',exact:true}).click();await p.screenshot({path:`artifacts/technique-layout-${width}.png`});console.log('PASS',width);await p.close();}}finally{await b.close();}


