import assert from 'node:assert/strict';import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{for(const width of [360,390,1440]){const p=await b.newPage({viewport:{width,height:844}});await p.goto('http://127.0.0.1:5173/#metronome',{waitUntil:'networkidle'});await p.getByRole('button',{name:'3 그루브',exact:true}).click();await p.getByRole('button',{name:'그루브팩 ▾',exact:true}).click();const d=p.getByRole('dialog',{name:'그루브팩',exact:true});const r=await d.locator('.groovePackRow').evaluateAll(es=>es.slice(0,3).map(e=>{const r=e.getBoundingClientRect();return{x:r.x,y:r.y};}));assert(Math.abs(r[0].y-r[1].y)<1&&r[1].x>r[0].x&&r[2].y>r[0].y);await d.getByRole('button',{name:'블루스 셔플',exact:true}).click();assert.equal(await d.getByRole('button',{name:'불러오기',exact:true}).isEnabled(),true);assert.equal(await d.evaluate(e=>e.scrollWidth>e.clientWidth),false);await p.screenshot({path:`output/groove-pack-two-columns-${width}.png`});await d.getByRole('button',{name:'불러오기',exact:true}).click();await d.waitFor({state:'hidden'});console.log('PASS',width);await p.close();}}finally{await b.close();}
