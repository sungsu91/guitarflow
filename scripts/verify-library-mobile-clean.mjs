import assert from 'node:assert/strict';import {mkdir} from 'node:fs/promises';
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE),b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});await mkdir('artifacts/library-mobile-clean',{recursive:true});let p;
try{for(const theme of ['light','brand']){
 const ctx=await b.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true});p=await ctx.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(theme=>localStorage.setItem('rifflabThemeMode',theme),theme);await p.goto('http://127.0.0.1:5173/#etudes');await p.getByLabel('PDF 파일 선택',{exact:true}).setInputFiles('C:/Users/User/Desktop/sheet music/Flower Dance.pdf');let d=p.getByRole('dialog',{name:'PDF 악보 정보',exact:true});await d.getByRole('button',{name:'기기에 저장',exact:true}).click();await d.waitFor({state:'hidden'});
 const baseline=await p.evaluate(async()=>{const {createBlankDocument}=await import('/src/etudes/scoreModel.js'),{saveLibraryDocument,markLibraryPracticed}=await import('/src/etudes/scoreLibrary.js'),{listPdfs,getPdf,fingerprintPdf}=await import('/src/pdf/pdfLibrary.js');const docs=[];for(const title of ['손가락 간격 첫걸음','나는 나비','왼손 연습','줄 이동']){const doc=createBlankDocument();doc.title=title;saveLibraryDocument(localStorage,doc);docs.push(doc);}markLibraryPracticed(localStorage,docs[1].id);localStorage.setItem('fretiva.score.folders.v1',JSON.stringify({version:1,folders:[{id:'f1',name:'핑거스타일'},{id:'f2',name:'교재의 아주 긴 폴더 이름도 악보 개수와 화살표를 침범하지 않기'}],locations:{['score:'+docs[2].id]:'f1',['score:'+docs[3].id]:'f2'}}));const pdf=(await listPdfs())[0];return {pdfId:pdf.id,pdfHash:await fingerprintPdf(await getPdf(pdf.id)),json:localStorage.getItem('fretiva.etude.library.v2')};});await p.reload();await p.getByRole('button',{name:'Flower Dance 열기',exact:true}).waitFor();
 assert.equal(await p.locator('.scoreFileColumns').count(),0);assert.equal(await p.locator('.scoreLibraryMenu').count(),0);assert.equal(await p.locator('.scoreFolderRows li').count(),2);assert.equal(await p.locator('.scoreMobileName strong').count(),3);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1),false);
 const shown=await p.locator('.scoreMobileName strong').evaluateAll(nodes=>nodes.filter(n=>{const r=n.getBoundingClientRect();return r.top>=0&&r.bottom<=innerHeight}).length);assert.equal(shown,3);await p.screenshot({path:`artifacts/library-mobile-clean/390-${theme}.png`});
 const filters=p.getByRole('navigation',{name:'악보 목록 필터'});await filters.getByRole('button',{name:'최근 연습',exact:true}).click();assert.equal(await p.locator('.scoreMobileName strong').count(),1);assert.equal(await p.locator('.scoreMobileName strong').innerText(),'나는 나비');await filters.getByRole('button',{name:'전체',exact:true}).click();
 await p.getByRole('button',{name:'목록 편집',exact:true}).click();await p.getByLabel('Flower Dance 관리').click();await p.locator('.scoreLibraryMenu[open]').getByRole('button',{name:'즐겨찾기 추가',exact:true}).click();await p.getByRole('button',{name:'목록 편집 완료',exact:true}).click();await filters.getByRole('button',{name:'즐겨찾기',exact:true}).click();assert.equal(await p.locator('.scoreMobileName strong').innerText(),'Flower Dance');
 await p.reload();await p.getByRole('navigation',{name:'악보 목록 필터'}).getByRole('button',{name:'즐겨찾기',exact:true}).click();assert.equal(await p.locator('.scoreMobileName strong').innerText(),'Flower Dance');await p.getByRole('navigation',{name:'악보 목록 필터'}).getByRole('button',{name:'전체',exact:true}).click();await p.getByRole('button',{name:'목록 편집',exact:true}).click();await p.getByRole('button',{name:'Flower Dance 선택',exact:true}).click();await p.getByRole('button',{name:'손가락 간격 첫걸음 선택',exact:true}).click();await p.getByRole('button',{name:'폴더로 이동 (2)',exact:true}).click();d=p.getByRole('dialog',{name:'선택 악보 이동'});await d.getByLabel('대상 폴더').selectOption('f1');await d.getByRole('button',{name:'이동',exact:true}).click();await d.waitFor({state:'hidden'});
 assert.equal(await p.evaluate(()=>localStorage.getItem('fretiva.etude.library.v2')),baseline.json);assert.equal(await p.evaluate(async id=>{const lib=await import('/src/pdf/pdfLibrary.js');return lib.fingerprintPdf(await lib.getPdf(id));},baseline.pdfId),baseline.pdfHash);
 await p.getByRole('button',{name:'핑거스타일 폴더 열기',exact:true}).click();assert.equal(await p.locator('.scoreMobileName strong').count(),3);await p.getByRole('button',{name:'Flower Dance 열기',exact:true}).click();await p.locator('.pdfPaper canvas').first().waitFor();await p.getByRole('tab',{name:'내 악보',exact:true}).click();await p.getByRole('button',{name:'목록 편집',exact:true}).click();await p.getByLabel('손가락 간격 첫걸음 관리').click();await p.locator('.scoreLibraryMenu[open]').getByRole('button',{name:'이름 변경',exact:true}).click();d=p.getByRole('dialog',{name:'악보 이름 변경'});await d.getByLabel('제목',{exact:true}).fill('운지 연습');await d.getByRole('button',{name:'저장',exact:true}).click();await d.waitFor({state:'hidden'});await p.getByLabel('운지 연습 관리').click();await p.locator('.scoreLibraryMenu[open]').getByRole('button',{name:'삭제',exact:true}).click();d=p.getByRole('dialog',{name:'확인',exact:true});await d.getByRole('button',{name:'취소',exact:true}).click();assert.equal(await p.getByRole('button',{name:'운지 연습 선택',exact:true}).count(),1);
 await p.getByRole('tab',{name:'에튀드',exact:true}).click();await p.locator('.etudeNotation svg').first().waitFor();assert.equal(await p.locator('.scoreLibraryAdd').count(),0);assert.equal(await p.getByRole('tab').count(),2);await p.getByRole('button',{name:'다음 연습곡',exact:true}).click();await p.screenshot({path:`artifacts/library-mobile-clean/390-${theme}-etudes.png`});assert.deepEqual(errors,[]);console.log({theme,visibleTitles:shown,favoritesPersist:true,recent:true,mixedMove:true,originalsPreserved:true,errors});await ctx.close();
}}catch(e){await p.screenshot({path:'artifacts/library-mobile-clean/failure.png'});throw e;}finally{await b.close();}
