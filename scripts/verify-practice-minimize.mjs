import assert from 'node:assert/strict';const{chromium}=await import('file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs');const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('tab',{name:'에튀드',exact:true}).click();assert.equal(await p.locator('.practiceEdgeTab').count(),0);await p.getByRole('button',{name:'메트로놈',exact:true}).click();await p.getByRole('button',{name:'연습 시작',exact:true}).click();await p.getByRole('button',{name:'메트로놈 닫기'}).click();const edge=p.getByRole('button',{name:'메트로놈 패널 펼치기'});await edge.waitFor();const box=await edge.boundingBox();await p.mouse.move(box.x+12,box.y+20);await p.mouse.down();await p.mouse.move(box.x+12,box.y+120,{steps:10});await p.mouse.up();assert.equal(await p.locator('.etudeRemote').count(),0);assert.ok((await edge.boundingBox()).y>box.y+50);await edge.click();await p.getByRole('button',{name:'일시정지',exact:true}).waitFor();await p.getByRole('button',{name:'메트로놈 닫기'}).click();await p.getByRole('button',{name:'메트로놈',exact:true}).click();assert.equal(await p.locator('.practiceEdgeTab').count(),0);assert.equal(await p.locator('.etudeRemote').count(),0);assert.equal(await p.getByRole('button',{name:'메트로놈',exact:true}).getAttribute('aria-pressed'),'false');await p.getByRole('button',{name:'백킹루프',exact:true}).click();await p.getByRole('button',{name:'백킹루프 패널 닫기'}).click();await p.getByRole('button',{name:'백킹루프 패널 펼치기'}).waitFor();await p.getByRole('button',{name:'백킹루프',exact:true}).click();assert.equal(await p.locator('.practiceEdgeTab').count(),0);console.log('PASS initially off, close minimizes, drag does not reopen, resume remains playing, toolbar disables both tools');}finally{await b.close();}
