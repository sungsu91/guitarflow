import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const browser=await chromium.launch({headless:true,channel:'msedge',args:['--use-fake-ui-for-media-stream','--use-fake-device-for-media-stream','--autoplay-policy=no-user-gesture-required']});
try{const page=await browser.newPage({viewport:{width:390,height:844},permissions:['camera','microphone']});await page.goto('http://127.0.0.1:5174/#shooter');await page.getByRole('button',{name:'촬영모드',exact:true}).click();await page.getByRole('button',{name:/^분할/}).click();await page.getByRole('button',{name:'● REC',exact:true}).waitFor();await page.evaluate(()=>{window.gaps=[];let prev=performance.now();function frame(t){gaps.push(t-prev);prev=t;requestAnimationFrame(frame);}requestAnimationFrame(frame);});await page.getByRole('button',{name:'● REC',exact:true}).click();await page.getByRole('button',{name:'녹화 중지',exact:true}).waitFor();console.log(await page.evaluate(()=>({maxGap:Math.max(...gaps),frames:gaps.length})));}finally{await browser.close();}

