import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{for(const width of [375,390]){const p=await b.newPage({viewport:{width,height:844}});await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('tab',{name:'에튀드',exact:true}).click();const print=p.getByRole('button',{name:'악보 PDF 저장 · 인쇄',exact:true});await print.waitFor();const a=await print.boundingBox(),f=await p.getByRole('button',{name:/즐겨찾기 (추가|해제)/}).boundingBox(),t=await p.getByRole('button',{name:'악보 표시 방식 변경',exact:true}).boundingBox();assert.ok(Math.abs(a.y-f.y)<3);assert.ok(Math.abs(t.y-f.y)<3);assert.ok(f.x+f.width<=width);console.log('PASS single row',width);await p.close();}}finally{await b.close();}
