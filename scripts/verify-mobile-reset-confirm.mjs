import assert from 'node:assert/strict';import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{const p=await b.newPage({viewport:{width:375,height:844}});await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('button',{name:'악보 만들기',exact:true}).click();await p.getByRole('button',{name:'프렛 3',exact:true}).click();const before=await p.locator('.etudeEditorMeasure svg').first().innerHTML();await p.getByRole('button',{name:'초기화',exact:true}).click();const prompt=p.getByRole('alertdialog',{name:'현재 마디 초기화'});await prompt.getByRole('button',{name:'아니요',exact:true}).click();assert.equal(await p.locator('.etudeEditorMeasure svg').first().innerHTML(),before);await p.getByRole('button',{name:'초기화',exact:true}).click();await prompt.getByRole('button',{name:'예',exact:true}).click();await prompt.waitFor({state:'hidden'});assert.notEqual(await p.locator('.etudeEditorMeasure svg').first().innerHTML(),before);console.log('PASS reset confirmation: no preserves score, yes resets current measure');}finally{await b.close();}
