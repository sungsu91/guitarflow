import assert from 'node:assert/strict';import {mkdir,writeFile} from 'node:fs/promises';const {chromium}=await import(process.env.PLAYWRIGHT_MODULE);const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});const p=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true,deviceScaleFactor:2});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5173/#etudes');await p.getByLabel('PDF 파일 선택',{exact:true}).setInputFiles('C:/Users/User/Desktop/sheet music/Flower Dance.pdf');const dialog=p.getByRole('dialog',{name:'PDF 악보 정보'});await dialog.getByRole('button',{name:'기기에 저장',exact:true}).click();await dialog.waitFor({state:'hidden'});await p.reload();await p.getByRole('button',{name:'Flower Dance 연습하기',exact:true}).waitFor();await p.locator('.pdfCard img').first().evaluate(img=>img.decode());await mkdir('artifacts/mobile-score',{recursive:true});await p.screenshot({path:'artifacts/mobile-score/390x844-library.png'});await p.getByRole('button',{name:'Flower Dance 연습하기',exact:true}).click();await p.getByLabel('PDF 확대',{exact:true}).selectOption('page');await p.waitForTimeout(800);await p.waitForFunction(()=>document.querySelector('.pdfViewport').getAttribute('aria-busy')==='false');const fit=await p.locator('.pdfViewport').evaluate(e=>({height:e.clientHeight,scrollHeight:e.scrollHeight,width:e.clientWidth,scrollWidth:e.scrollWidth}));assert.ok(fit.scrollHeight<=fit.height+1);assert.ok(fit.scrollWidth<=fit.width+1);await p.getByRole('button',{name:'‹ 내 악보 보관함',exact:true}).click();await p.reload();await p.getByRole('button',{name:'Flower Dance 연습하기',exact:true}).click();assert.equal(await p.getByLabel('PDF 확대',{exact:true}).inputValue(),'page');assert.deepEqual(errors,[]);await writeFile('artifacts/mobile-score/pdf-page-fit.json',JSON.stringify({fit,restored:true,errors},null,2));await browser.close();
