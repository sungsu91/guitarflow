import assert from 'node:assert/strict';import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{const p=await b.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('button',{name:'악보 만들기',exact:true}).click();for(let i=0;i<3;i++)await p.getByRole('button',{name:'마디 추가',exact:true}).click();await p.getByRole('button',{name:'한 줄 4마디',exact:true}).click();for(const bar of [0,2,1]){const section=p.locator(`[data-bar-index="${bar}"]`);const xy=await section.evaluate(e=>{const svg=e.querySelector('div').shadowRoot.querySelector('svg');const hit=svg.querySelector('[data-mode="tab"][data-cursor-y]');const pt=svg.createSVGPoint();pt.x=2;pt.y=Number(hit.dataset.cursorY);const screen=pt.matrixTransform(svg.getScreenCTM());return {x:screen.x,y:screen.y};});await p.touchscreen.tap(xy.x,xy.y);await p.waitForTimeout(100);assert.match(await p.locator('.mobileCursorPad output').textContent(),new RegExp(`${bar+1}마디 · 1박`));}console.log('PASS: editor 4-bar TAB headers select corresponding first beat');}finally{await b.close();}
