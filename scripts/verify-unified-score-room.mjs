import {chromium} from 'file:///C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import {createBlankDocument} from '../src/etudes/scoreModel.js';
import assert from 'node:assert/strict';
import {mkdir,writeFile} from 'node:fs/promises';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});await mkdir('artifacts/unified-score-room',{recursive:true});const report=[];
try{for(const width of [390,1440]){
 const p=await browser.newPage({viewport:{width,height:956},isMobile:width<600,hasTouch:width<600});globalThis.page=p;p.setDefaultTimeout(15000);const errors=[];p.on('pageerror',e=>errors.push(e.message));const d=createBlankDocument();d.title='정리 테스트 악보';
 await p.addInitScript(d=>{if(!localStorage.getItem('fretiva.etude.library.v2'))localStorage.setItem('fretiva.etude.library.v2',JSON.stringify({version:2,records:{[d.id]:{document:d,status:'saved'}}}));},d);
 await p.goto('http://127.0.0.1:5173/#etudes');await p.getByRole('heading',{name:'악보연습실',exact:true}).waitFor();assert.equal(await p.getByRole('tab',{name:'내 악보',exact:true}).count(),0);const actions=p.locator('.scoreWorkspaceActions');await actions.getByRole('button',{name:'생성',exact:true}).click();await p.locator('[data-ui=score-editor]').waitFor();if(width<600)await p.keyboard.press('Escape');else await p.locator('[data-ui=score-editor]').getByRole('button',{name:'닫기',exact:true}).click();await p.locator('[data-ui=score-editor]').waitFor({state:'hidden'});
 await p.locator('.etudePickerTrigger').nth(1).click();const picker=p.locator('.etudePickerDialog');const manage=p.locator('.pickerManageDialog');const cats=picker.locator('.etudePickerCategories');
 await cats.getByRole('button',{name:'새 폴더',exact:true}).click();await manage.getByRole('textbox').fill('매일 연습');await manage.getByRole('button',{name:'저장',exact:true}).click();await manage.waitFor({state:'hidden'});
 await cats.getByRole('button',{name:'전체',exact:true}).click();await picker.getByRole('button',{name:'악보 정리',exact:true}).click();await picker.locator('.etudePickerCard').filter({hasText:d.title}).click();await picker.getByRole('button',{name:'폴더 이동 (1)',exact:true}).click();await manage.getByRole('combobox').selectOption({label:'매일 연습'});await manage.getByRole('button',{name:'이동',exact:true}).click();await manage.waitFor({state:'hidden'});
 await cats.getByRole('button',{name:'매일 연습',exact:true}).click();assert.equal(await picker.locator('.etudePickerCard').count(),1);await picker.locator('.etudePickerCard').click();await picker.locator('.etudePickerManage').getByRole('button',{name:'즐겨찾기 +',exact:true}).click();
 await picker.locator('.etudePickerManage').getByRole('button',{name:'이름 변경',exact:true}).click();await manage.getByRole('textbox').fill('매일 하이햇');await manage.getByRole('button',{name:'저장',exact:true}).click();await manage.waitFor({state:'hidden'});assert.match(await picker.locator('.etudePickerCard').innerText(),/매일 하이햇/);
 await p.screenshot({path:`artifacts/unified-score-room/${width}-organize.png`});await picker.getByRole('button',{name:'악보 정리',exact:true}).click();await picker.locator('.etudePickerCard').click();await picker.locator('footer button').click();assert.equal(await p.locator('.etudePickerTrigger.is-current-score').getAttribute('title'),'매일 하이햇');
 await actions.getByRole('button',{name:'편집',exact:true}).click();await p.locator('[data-ui=score-editor]').waitFor();if(width<600)await p.keyboard.press('Escape');else await p.locator('[data-ui=score-editor]').getByRole('button',{name:'닫기',exact:true}).click();await p.locator('[data-ui=score-editor]').waitFor({state:'hidden'});
 await p.screenshot({path:`artifacts/unified-score-room/${width}-room.png`});await p.reload();await p.locator('.etudePickerTrigger').nth(1).click();await cats.getByRole('button',{name:'매일 연습',exact:true}).click();assert.match(await picker.locator('.etudePickerCard').innerText(),/매일 하이햇/);
 await picker.getByRole('button',{name:'악보 정리',exact:true}).click();await picker.getByRole('button',{name:'폴더 이름 변경',exact:true}).click();await manage.getByRole('textbox').fill('루틴');await manage.getByRole('button',{name:'저장',exact:true}).click();await manage.waitFor({state:'hidden'});await picker.getByRole('button',{name:'폴더 삭제',exact:true}).click();await manage.getByRole('button',{name:'삭제',exact:true}).click();await manage.waitFor({state:'hidden'});assert.equal(await cats.getByRole('button',{name:'루틴',exact:true}).count(),0);assert.match(await picker.locator('.etudePickerCard').innerText(),/매일 하이햇/);
 await picker.getByRole('button',{name:'악보 선택 닫기',exact:true}).click();
 await actions.getByRole('button',{name:'PDF 불러오기',exact:true}).click();await p.getByLabel('PDF 파일 선택',{exact:true}).setInputFiles('artifacts/omr-prototype/clean-staff-one-page.pdf');await p.getByRole('dialog',{name:'PDF 악보 정보',exact:true}).waitFor();const info=p.getByRole('dialog',{name:'PDF 악보 정보',exact:true});await info.getByRole('textbox').first().fill('PDF 루틴');await info.getByRole('button',{name:'기기에 저장',exact:true}).click();await p.getByRole('button',{name:'악보연습실로 돌아가기',exact:true}).waitFor();await p.getByRole('button',{name:'악보연습실로 돌아가기',exact:true}).click();await p.locator('.etudePickerTrigger').nth(1).click();assert.equal(await picker.locator('.etudePickerCard').filter({hasText:'PDF 루틴'}).count(),1);
await picker.getByRole('button',{name:'악보 정리',exact:true}).click();await picker.locator('.etudePickerCard').filter({hasText:'PDF 루틴'}).click();await picker.locator('.etudePickerManage').getByRole('button',{name:'이름 변경',exact:true}).click();await manage.getByRole('textbox').fill('PDF 변경');await manage.getByRole('button',{name:'저장',exact:true}).click();await manage.waitFor({state:'hidden'});assert.equal(await picker.locator('.etudePickerCard').filter({hasText:'PDF 변경'}).count(),1);
await picker.getByRole('button',{name:'악보 삭제',exact:true}).click();await manage.getByRole('button',{name:'취소',exact:true}).click();assert.equal(await picker.locator('.etudePickerCard').filter({hasText:'PDF 변경'}).count(),1);await picker.getByRole('button',{name:'악보 삭제',exact:true}).click();await manage.getByRole('button',{name:'삭제',exact:true}).click();await manage.waitFor({state:'hidden'});assert.equal(await picker.locator('.etudePickerCard').filter({hasText:'PDF 변경'}).count(),0);assert.equal(await picker.locator('.etudePickerCard').filter({hasText:'매일 하이햇'}).count(),1);
 assert.deepEqual(errors,[]);report.push({width,errors});console.log('PASS',width);await p.close();
}}catch(e){if(globalThis.page&&!globalThis.page.isClosed()){await globalThis.page.screenshot({path:'artifacts/unified-score-room/failure.png'});console.log((await globalThis.page.locator('body').innerText()).slice(-4500));}throw e;}finally{await writeFile('artifacts/unified-score-room/verification.json',JSON.stringify(report,null,2));await browser.close();}
